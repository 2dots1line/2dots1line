{
  "name": "@2dots1line/core-utils",
  "version": "0.1.0",
  "description": "Core utilities for 2dots1line V7",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm clean && tsc -p tsconfig.build.json",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "jest",
    "clean": "node -e \"const fs=require('fs'); try{fs.rmSync('dist',{recursive:true,force:true})}catch{}; try{fs.rmSync('tsconfig.build.tsbuildinfo',{force:true})}catch{}\""
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "date-fns-tz": "^2.0.0",
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.17.0",
    "@types/uuid": "^9.0.7",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}